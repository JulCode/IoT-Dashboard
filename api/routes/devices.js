const express = require("express");
const router = express.Router();

router.get("/device", (req, res) => {}); // get all devices
router.post("/device", (req, res) => {}); // create new device
router.delete("/device", (req, res) => {}); //delete device
router.put("/device", (req, res) => {}); //update device

module.exports = router;
